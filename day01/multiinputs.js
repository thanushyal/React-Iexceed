//multiple inputs in functional component

import React from "react";

function Multiple({ name, rollno }) {
    return (

        <
        p > < h2 > MULTIPLE INPUTS in functional component < /h2>Name is {name} <br / > rollnumber is { rollno } < /p>)
    }
    export default Multiple;