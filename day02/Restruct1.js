//destructing of props in functional components

import React from "react";

function Restrut1({ bankname, amount }) {

    return ( < >

        <
        p > < h1 > destructing of props in function < /h1>name of the bank { bankname } < /p > <
        p > AMOUNT: { amount } < /p></ > );

}
export default Restrut1;